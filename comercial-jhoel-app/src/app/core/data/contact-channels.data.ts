/**
 * `CONTACT_CHANNELS`/`SOCIAL_LINKS` used to live here as hardcoded
 * placeholders — removed once `ContactService` started building them
 * dynamically from the real `company_settings` data (`PublicCompanyInfo`,
 * via `CompanySettingsService.getPublicInfo()`). See
 * `core/services/contact.service.ts`.
 */
export const CONTACT_INTERESTS: string[] = [
  'Útiles escolares',
  'Fotocopias e impresiones',
  'Agente bancario',
  'Servicios administrativos',
  'Venta de teléfonos',
  'Otro',
];
